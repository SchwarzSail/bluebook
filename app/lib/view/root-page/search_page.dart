import 'package:flutter/material.dart';

class SearchPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('搜索项目'),
      ),
      body: Center(
        child: Text('搜索项目功能还在开发中...'),
      ),
    );
  }
}
